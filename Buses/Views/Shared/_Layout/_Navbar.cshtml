<nav class="navbar navbar-expand-lg navbar-light fondoNavbar">
    <div class="container">
        <a class="navbar-brand text-white font-weight-bold" asp-action="Index" asp-controller="PaginaPrincipal">Red Bus</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link text-white font-weight-bold" href="#">Destinos<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white font-weight-bold" asp-controller="Corporativo" asp-action="Index">Corporativo</a>
                </li>
            </ul>
        </div>
        <form class="form-inline my-2 my-lg-0 float-right">
            <img src="~/images/linkedin (1).png" width="30" class="mr-4" id="linkedin" />
            <img src="~/images/youtube (1).png" width="30" class="mr-4" id="youtube" />
            <img src="~/images/variante-del-logo-de-whatsapp.png" width="30" class="mr-4" id="whatsapp" />
            <img src="~/images/instagram (1).png" width="30" class="mr-4" id="instagram" />
            @if (User.Identity.IsAuthenticated)
            {
                <button type="button" id="registrames" class="btn btn-outline-light my-2 ml-1 mr-1 my-sm-0 font-weight-bold">
                    <span class="mr-1">
                        <img src="~/images/usuario (1).png" width="20" />
                    </span>
                    @User.Identity.Name
                </button>
                <a asp-action="Logout" asp-controller="Account" class="btn btn-outline-light mr-1 my-2 my-sm-0 font-weight-bold">Cerrar Sesion</a>

            }
            else
            {
                <button style="" type="button" class="btn btn-outline-light mr-1 my-2 my-sm-0 font-weight-bold" data-toggle="modal" data-target="#ModalIniciarSesion">
                    Iniciar Secion
                </button>
                <button type="button" id="" class="btn btn-outline-light my-2 ml-1 my-sm-0 font-weight-bold" data-toggle="modal" data-target="#ModalRegistrarme">
                    Registrarme
                </button>
            }
        </form>

    </div>
</nav>